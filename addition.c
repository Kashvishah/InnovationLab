#include <stdio.h>
int main()
{
    int num1,num2,sum=0;
    printf(" enter the number num1 and num2 :");
    scanf("%d%d", &num1, &num2);
    sum=num1+num2;
    printf(" the addition of two numbers are %d",sum);
}
